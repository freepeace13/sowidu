import { StyleSheet } from "react-native"

export default StyleSheet.create({
  titleContainer: {
    alignItems: "center",
    justifyContent: "center",
    flexDirection: "row",
    columnGap: 8,
    paddingHorizontal: 8,
    paddingVertical: 14,
  },
})
