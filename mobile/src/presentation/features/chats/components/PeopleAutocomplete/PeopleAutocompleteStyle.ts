import { StyleSheet } from "react-native"

export default StyleSheet.create({
  container: {
    flexWrap: "wrap",
    flexDirection: "row",
    padding: 10,
    gap: 4,
    borderBottomWidth: 1,
  },
  textInput: {
    flexGrow: 1,
    height: 20,
    backgroundColor: "transparent",
  },
})
