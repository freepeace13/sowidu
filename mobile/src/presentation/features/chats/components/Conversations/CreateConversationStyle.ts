import { StyleSheet } from "react-native"

export default StyleSheet.create({
  footer: {
    flexDirection: "row",
    justifyContent: "flex-end",
    borderTopWidth: 1,
    padding: 12,
    gap: 16,
  },
})
