import { StyleSheet } from "react-native"

export const DEFAULT_BACKGROUND = "#f8f9fb"

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: DEFAULT_BACKGROUND,
  },
})
