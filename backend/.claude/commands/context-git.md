# Context: Git

Load the git context for working on branches, commits, and pull requests.

## Instructions

Read and present the contents of `.claude/context/git.md` to provide context for the current task. This context covers:

- Branch naming conventions
- Commit message formats
- PR checklist and requirements
- Git workflow guidelines
