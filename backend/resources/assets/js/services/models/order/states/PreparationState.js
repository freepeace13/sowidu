/** @flow */

import hexColors from '~/enums/hexColors';
import State from '../../fundamentals/state';

class PreparationState extends State {
    key: string = 'preparation';
    color: string = hexColors.BLUE_GREY;
}

export default PreparationState;